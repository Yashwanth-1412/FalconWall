#include "falcon/core/stats.hpp"

namespace falcon {

// Placeholder for framework-level helpers that do not belong to a single
// backend (e.g. version string, common formatting). Milestone 1 fills this in
// alongside the PacketIO interface.

} // namespace falcon
